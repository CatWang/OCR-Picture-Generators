这个目录里面的内容都与中文字符生成有关

注意：dict字典文件在梁鼎caffe版本的OCR模型中是作为输入文件的

1. common文件夹中的dict-common包含了全部的一级汉字和部分二级汉字，一共5570个字
	该文件中不包含特殊字符等。

2. common文件夹中存放的是使用dict-6000生成普通字符图片的代码，包含正常、扰动和水印。
	内含demo_image

3. Piaoju-background文件夹中存放的是从票据中裁下来的背景，可以用来生成以票据背景的字符图片

4. piaoju-med文件夹中存放的是利用票据背景和文字生成相应字符图片的代码
	其中的文字的参考来源是国家药品清单和现有票据真实数据集中的词组。
	文字包括之前的dict-common中的文字和国家药品清单中文字的并集，
	以及真实数据集中的词组。

5. piaoju-med中的dict-med.txt文件就是包含医学票据常用字的和常用特殊字符的词典
	其中包含dict-common中的全部文字
	医疗票据中的常见字（部分与dict-common重叠，所以重叠部分只取一次）
	医疗票据中的常见特殊字符或者字母，例如Ⅲ、×、+等。

6. piaoju-med中的生成文字图片代码generate-normal.py中，使用的字符来源是在dict-med的基础上，
	又添加了一些真实医疗票据中的常见词组。
	且在背景图片中，字符打印的位置是随机的。

7. piaoju-points中的代码是用来模拟针式打印效果的

8. BigTest中的代码是用来做OCR-detection测试集的
	因为当时发现一个图里面如果全是各种字的搭配的话，检测起来反而效果不好
	依旧是颜色随机，大小随机，按行紧密排列
	示例效果在文件夹下的demo-image中

9. Detection中的代码是用来在图片上写入多行分开的文字，做图片中的文字检测用的
	
